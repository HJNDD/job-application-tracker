name: CI (Docker Build)

on:
  push:
  pull_request:

concurrency:
  group: ci-docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Compose build (backend compose file)
        run: |
          docker compose -f backend/docker-compose.yml build
name: CI (Docker Build)

on:
  push:
  pull_request:

concurrency:
  group: ci-docker-${{ github.ref }}
  cancel-in-progress: true

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Docker Compose build (backend compose file)
        run: |
          docker compose -f backend/docker-compose.yml build
